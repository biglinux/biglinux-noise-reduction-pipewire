# Optional metric. Install with `pip install -r requirements-extra.txt`
# after the base venv is up. It brings native code that may need a
# matching Python ABI.
#
# pesq — ITU-T P.862 wideband; needs a C compiler.
pesq>=0.0.4
